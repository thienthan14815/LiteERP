{
  "name": "@app/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and DTOs between FE and BE",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'shared package: lint placeholder'"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
